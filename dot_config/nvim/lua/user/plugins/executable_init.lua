-- Use this file to list plugins that don't need further configuration.

return {
    "nvim-lua/plenary.nvim", -- lua functions that many plugins use
    -- "christoomey/vim-tmux-navigator", -- tmux & split window navigation
}
